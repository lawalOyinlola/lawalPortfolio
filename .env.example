# dev.to Articles API key, used by `pnpm sync:devto`.
# Generate at dev.to: Settings > Extensions > DEV Community API Keys.
# Copy this file to .env.local and fill it in. Never commit the real key.
DEVTO_API_KEY=
